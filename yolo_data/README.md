# Prepared YOLO dataset placeholder

This folder is generated locally by `prepare_yolo_data.py`; it is not committed.
After preparation it contains `data.yaml` and the `images/` and `labels/` train,
validation, and test splits.
